/**
 * @deepseek-ai/dsh-im — the optional IM integration as a profile bundle. The
 * package's substance is `cordis.patch.yml`, declared by the `dsh.bundle.patch`
 * manifest field and resolved by the profile composer through that field; it
 * pins the `xmanrui-dsh-im` row over any surface profile, enabling the pinned
 * `@xmanrui/dsh-im` plugin. This module carries no runtime API.
 * @module @deepseek-ai/dsh-im
 */

export {}
