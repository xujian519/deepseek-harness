/**
 * Host plugin body — no host-side behavior for this source plugin. The browser
 * half (`./client`) folds `patent-teams/*` session events; this entry exists
 * so the Loader tree carries the package row.
 * @module @deepseek-ai/dsh-client-ui-patent-teams
 */
export function apply(): void {}
