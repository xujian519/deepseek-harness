version: 1
scenario: document-deliver
profile: headless
composition: document-deliver
recording: live
header:
  class: document-deliver
  pin: true
input:
  task: |
    登记本次交付物：先调用 document_deliver 登记 out/draft.md（files 用 [{ path: "out/draft.md", format: "markdown" }]，
    gate.p0 用 ["命名规范"]，char_budget 用 500）；如果这次调用被拒绝，再登记 out/final.md（其余参数相同）。
    最后用一句话说明两次调用的差别，不要重复文件内容。
