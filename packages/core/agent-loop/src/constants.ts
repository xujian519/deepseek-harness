/** Shared agent-loop scheduler defaults.
 * @module dsh-agent-loop/constants
 */

import { DEFAULT_MAX_PARALLEL_SUB_CALLS } from '@deepseek-ai/dsh-tools'

/** Default maximum in-flight parallel-safe calls per agent step. */
export const DEFAULT_MAX_PARALLEL_TOOL_CALLS = DEFAULT_MAX_PARALLEL_SUB_CALLS
